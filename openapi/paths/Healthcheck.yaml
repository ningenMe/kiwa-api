get:
  tags:
    - healthcheck
  parameters: [ ]
  operationId: healthcheckGet
  responses:
    '200':
      description: ok
      content:
        application/json:
          schema:
            $ref: '../responses/HealthcheckGetResponse.yaml'

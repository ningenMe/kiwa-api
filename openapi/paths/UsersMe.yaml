get:
  tags:
    - users
  parameters: [ ]
  operationId: usersGet
  responses:
    '200':
      description: ok
      content:
        application/json:
          schema:
            $ref: '../responses/UsersMeGetResponse.yaml'
    '401':
      description: unauthenticated 401
      content:
        application/json:
          schema:
            $ref: '../responses/UnAuthenticated401.yaml'
